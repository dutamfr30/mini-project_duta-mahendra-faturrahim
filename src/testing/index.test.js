import { render } from "@testing-library/react";
import Home from '../pages/index';

describe('Home', () => {
    test('render first', () => {
        render(<Home/>);
    });
    
    // test('Testing Welcome', () => {
    //     render
    // })
})